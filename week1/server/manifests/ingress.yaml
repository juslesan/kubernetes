apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: server-response-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: server-svc
          servicePort: 2345